<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoffeeSh0p"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" x:Class="CoffeeSh0p.OrderWindow"
        mc:Ignorable="d"
        Height="660" Width="825"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterScreen">
    <Grid>
        <Border Background="#272537" CornerRadius="20">
            <StackPanel>

                <TextBlock Text="Ваш заказ"
                   Foreground="White"
                   FontSize="28"
                   HorizontalAlignment="Center"
                   Margin="0,50,0,0" x:Name="drink"/>
                <DatePicker x:Name="DP" Margin="0,20,0,0" Width="200" FirstDayOfWeek="Monday" SelectedDateFormat="Long"/>

                <xctk:DateTimeUpDown/>

            </StackPanel>
        </Border>
    </Grid>
</Window>
